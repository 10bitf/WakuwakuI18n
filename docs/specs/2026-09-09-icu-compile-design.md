# 复数与 ICU：构建期编译 —— 设计（2026-09-09）

> 起因在 Racing：做海外版时发现 i18next 的复数选择只认 `count` 这个魔法变量名，
> 而全仓的占位符叫 `{n}`；一条消息还只能有一个复数。用户裁定「改 wakuwaku-i18n，做到最好」。
>
> **这份文档不是实现，是把方案、契约变更、验收单钉住。** 动手前先过 spike（见 §7）。

---

## 1. 问题：三个，但只有一个是真的急

| # | 问题 | 严重度 |
|---|---|---|
| 1 | i18next 的复数选择**只认 `count`**，我们的占位符叫 `{n}` → 加复数那天，没传 `count` 的调用点**渲染成空白** | 🔴 静默 |
| 2 | **一条消息只能有一个复数**。Racing 有 9 条形如 `{races} 场 · {winners} 位分站冠军 · {drivers} 位车手` | 🔴 做不了 |
| 3 | 序数（英语 1st/2nd/3rd/**11th**）要另一套后缀 `_ordinal_*` | ◐ |

⚠️ **问题 1 最坏的部分是我们自己造的**：i18next 缺 `count` 时返回 key 本身（屏幕上一坨乱码，看得见），
而各消费方的 `parseMissingKeyHandler` 把它换成了空串（「缺 key 绝不把 key 显示给用户」）。
那条规矩本身对，副作用是**把库唯一的提示也吞了**。

> 记这一条不是为了追责，是因为它决定了修法：**我们吞掉了库的提示，就得自己补一个回来**
> （Racing 2026-09-08 已在取词层加了「占位符没填上」的开发期哨兵，那是止血不是治本）。

---

## 2. 《现成方案调研》

> 用户全局 CLAUDE.md 的造轮子闸门：i18n 属水电类，**没有这一节不许进实现**。

### 2.1 三条路

| 方案 | 代表 | 满足硬需求 | 体积 | 不用它的**具体**理由 |
|---|---|---|---|---|
| **A 运行时解析 ICU** | FormatJS / react-intl · `i18next-icu` | 🔴 **不满足** | polyfill **1.7 MB** | **依赖 `Intl.PluralRules`，而安卓微信基础库没有 `Intl`** —— Racing 的 `core/tz.js` 早有 plat-limit 记着这条（`dayjs-tz`/`luxon`/`date-fns-tz` 全因此不能用）。polyfill 装不下：Racing 主包只剩约 450 KB 余量 |
| **B 构建期编译** | **Lingui**（React 圈「性能党首选」）· `@messageformat/core` | ✅ 全满足 | 🎉 **净 −33 KB** | ✅ **选它**（理由见 §3；体积是 2026-09-09 spike 打包实测，**比预估的 +7 KB 好得多** —— 因为 i18next 整个退出了）|
| C 各家自有复数 | i18next 的 `_one`/`_other` | ◐ 现状 | 0 | 就是问题 1/2/3 本身 |

### 2.2 那 MessageFormat 2.0（「最新标准」）呢

| | |
|---|---|
| 规范状态 | Unicode **Final Candidate（2025-03）**，spec-stable |
| 生产采用 | 🔴 **2026 年仍然罕见** |
| 进 JS 标准的门槛 | TC39 要**约十家公司在生产用过**才继续推进 —— **未达到** |
| 主流框架 | **一个都没设为默认**；FormatJS / react-intl 仍在 ICU 1 |
| 工具链 | 翻译平台与译员编辑器**还不认 MF2** |

**结论：MF2 是未来不是现在。** 现在上等于当小白鼠，且译员用不了。

✅ **但选 `@messageformat` 家族等于给 MF2 留门** —— MF2 的 JS 实现（`messageformat@4`）
就是同一批人维护的，将来迁移在同一个生态内。

### 2.3 一人日闸

不是自建，是接现成的编译器 + 一个 Vite/Astro 插件。**未超一人日**（估 0.5～1 人日 + spike）。

---

## 3. 实测数（2026-09-09 跑出来的，不是推的）

拿 Racing 的真实文案表（`miniapp/i18n/zh/`，926 条）跑 `@messageformat/core@3.4.0`：

| 问题 | 读数 |
|---|---|
| 现有 926 条是不是合法 ICU | 🎉 **926 / 926 是。一条都不用改** |
| 编译产物依赖 `Intl` 吗 | ✅ **不依赖**（118 字节/条纯函数，零 `Intl` 引用；en/de/ar 三个语种都验过）|
| 体积 | 33 KB → 40 KB（**×1.2**）。只编译**真的用到 ICU 特性**的那些则增量 ≈ 0 |
| 一条消息多个复数 | ✅ 实测 `{races, plural, ...} · {drivers, plural, ...}` → `1 races · 20 drivers` |
| 序数 | ✅ `selectordinal` |
| 中文表误写 `one` | ✅ **编译期直接报错**（`The plural case one is not valid in this locale`）—— 中文只有 `other` |

**关键推论：`{n} 站` 本身就是合法 ICU**（`{n}` 是简单参数）。所以：

- 中文表**一个字不用改**
- **魔法 `count` 名消失** —— ICU 把变量名写在消息里：`{n, plural, one{…} other{…}}`
- Racing 那 75 条 `{n}` → `{count}` 的迁移**直接作废，不用做了**

---

## 4. 🔴 这是身份变更，不只是 API 变更

README 第一句写着框架的自我定位：

> 「它**不提供取词**，取词交给 i18next」

本方案**正面改这一条**。改完之后框架管五件事，不是四件：

| | 现在 | 以后 |
|---|---|---|
| 禁裸中文 `lint-raw` | ✅ | ✅ 不动 |
| 一致性检查 `check` | ✅ | ◐ 小改（要认编译产物）|
| 豁免通道 `exempt` | ✅ | ✅ 不动 |
| 扫描 `scan` | ✅ | ✅ 不动 |
| **取词 / 格式化** | ❌ 交给 i18next | 🔴 **本框架提供**（编译产物 + 极薄运行时）|

**这一条必须显式裁定，不能顺手改掉** —— 它是框架 README 的第一句话。

---

## 5. 契约变更

### 5.1 对外导出

| 入口 | 变化 |
|---|---|
| `wakuwaku-i18n/load` | 🔴 **语义变**：现在返回**数据**（JSON 对象），以后返回**函数表**。**Photoman / Wakuwaku / Dirty 三个项目在用** |
| `wakuwaku-i18n/i18next-preset` | 🔴 **可能整个退役**（i18next 不再参与取词）。5 个项目在用 |
| `wakuwaku-i18n/scan` · `/exempt` | 不动 |
| **新增** 编译入口（CLI + 插件） | 新 |

⚠️ `tools/doc-check.mjs` 机械校验 USAGE.md 第 4/5 节的契约表 —— **改导出必须同步改表，否则 `npm test` 直接红**。
这是好事：它保证契约变更不会悄悄发生。

### 5.2 消费方现状（2026-09-09 扫的）

| 项目 | 用到的入口 | 形态 |
|---|---|---|
| Racing | `i18next-preset` · `scan` · `tools/*` | uni-app 小程序 🔴 **唯一的无 `Intl` 环境** |
| Photoman | 上述 + **`load`** | 小程序 + 官网 |
| Wakuwaku | 上述 + **`load`** + 自建 hot-reload 插件 | Astro 静态站 |
| Dirty | 上述 + **`load`** | — |
| WakuwakuDark | 同 Racing | — |
| wakuwaku-kit | 仅依赖声明 | — |

---

## 6. 明确不改的

- `lint-raw` 的判据与豁免通道（`i18n-exempt-line`）
- `analyze` 的**占位符按出现次数比对**（2026-09-08 刚补了复数变体支持）
- `i18n.config.mjs` 的字段语义（`locales` / `scan` / `rawLint` / `assets`）
- **Wakuwaku 的批注 app**：它只把「元素 + 意见 + 源码位置」写进 `.annotations/pending.json`，
  **碰都不碰取词逻辑** —— 不受影响

---

## 7. 🔴 验收单：spike 要回答的三件事

按用户全局 CLAUDE.md 的实测闸，**写不出判读表就不许开工**。三件都只有跑一趟才知道。

| # | 问题 | 判读 |
|---|---|---|
| 1 | 编译步骤接得进 uni-app 的构建吗 | 接得进 → 继续；接不进 → 方案作废，回 §2 重选 |
| 2 | **小程序真机**跑得通吗 | 跑通 → 「无 `Intl`」这条硬约束解除；跑不通 → **看报错是不是 `Intl`**：是 → 编译没编干净；不是 → 另一个问题，别混为一谈 |
| 3 | 🔴 **改 JSON → 浏览器立刻变，这条不能坏** | 立刻变 → 过；要手动重启 → 不合格（现在 Wakuwaku 是重启 dev server，**新方案该更好不该更差**）|

⚠️ 第 3 条是用户 2026-09-09 追加的（「浏览器上直接改文字之类的」）。
**没有它，症状是「改了文字页面没反应，还以为改错了地方」** —— 那种问题最难查。

⚠️ 体积要**打出包来量**，不是拿 node_modules 的大小推算。

---

## 8. 风险与代价

1. **换的是运行时，不是加插件。** 虽然各消费方对 i18next 用得都很浅（只有取词 + 缺 key 兜底），但那是全站取词路径。
2. **4 个项目要一起验**，不是改完就完。Racing 是唯一的小程序（无 `Intl`），最该先验。
3. **多一个构建环节 = 多一个会坏的地方。** 编译失败要报得清楚，别变成「构建挂了但看不出为什么」。
4. **MF2 将来要再迁一次**（但那时全行业一起迁，且同生态内）。
5. ⚠️ **收益全在未来**：中文项目不需要复数（中文只有 `other`）。今天改完，界面一模一样。
   —— 所以它**不急**；急的是别在做外语版之前把方向定错，而方向现在定对了。

---

## 9. 分步走

| 步 | 做什么 | 出口 |
|---|---|---|
| **0** | 本文档 + 用户裁定 §4 的身份变更 | 定了才往下 |
| **1** | ~~在 Racing 上做 spike~~ **2026-09-09 做完，见 §11** | ①③ 过，②（真机）待跑 |
| **2** | ~~spike 绿 → 改上游框架，USAGE 契约表同步~~ **2026-09-09 做完，v0.5.0** | `npm test` 142 绿；顺手补了 doc-check 的反方向（此前删掉一整行契约表照样全绿）|
| **3** | 逐个项目升级并验，Racing 先（它是唯一无 `Intl` 的）| 各项目 gates 绿 |

**步 3 进度（2026-09-09）**：Racing 的 spike 分支 `i18n-icu-spike` 已改用框架 v0.5.0，
仓内那两份拷贝（`miniapp/tools/i18n-compile.mjs`、`src/i18n/make-t.js`）删掉。
读数：miniapp 单测 1238 绿、`tools/gates.mjs` 全绿、`build:mp-weixin` 通过且**产物里 `Intl.` 出现 0 次**。
欠：真机验收②（`pkg-dev/i18ntest` 的取词引擎自检卡三行读数），实现刚换过，得重传体验版再看一趟。
其余项目仍在 v0.3.0，按 §10.3 各自的节奏迁，不设死截止。

**spike 红了就回 §2 重选，不硬上。**

---

## 10. 其它项目怎么办（2026-09-09 补）

### 10.1 先分清：**换的动机只有出海项目才有**

| 项目 | 形态 | 有复数需求吗 | 换的收益 |
|---|---|---|---|
| **Racing** | uni-app 小程序 | 🔴 **有**（海外版 app 在计划里）| 大：复数 + 序数 + 一句多复数 |
| Photoman | 小程序 + 官网 | 暂无 | 小：包更小、构建期报错 |
| Wakuwaku | Astro 静态站 | 暂无 | 小：同上 |
| Dirty · WakuwakuDark | — | 暂无 | 小 |

⚠️ 但「收益小」不等于「没收益」。Racing 实测：换掉 i18next 之后
**`vendor.js` 746 → 703 KB，净省约 33 KB**。而且现有文案 **926/926 本来就是合法 ICU**，
迁移成本主要在**接构建插件**，不在改文案。

### 10.2 框架提供什么、宿主提供什么

**这是本节最要紧的一条**：三个宿主的构建系统不一样，插件写不了一份。

| | 谁提供 | 说明 |
|---|---|---|
| **编译器**（表 → 函数模块）| ✅ **框架** | 纯函数，与构建系统无关。Racing spike 里那份 `i18n-compile.mjs` 就是它的原型 |
| **接线**（何时编、怎么热更）| 🔴 **各宿主自己** | uni-app 是 Vite 插件；Astro 是另一个 Vite 插件；Photoman 官网走 `build-site.mjs`，压根不是 Vite |
| **取词纯逻辑**（`makeT`）| ✅ 框架 | 缺 key 兜空串、`vars` 缺省兜 `{}`、开发期哨兵 —— 三家一样 |

⚠️ **框架不要试图统一「接线」那一层。** 那是三套构建系统的差异，硬统一的结果是一个
到处是 if 的插件，而且哪家构建升级都可能把它弄坏。

### 10.3 迁移顺序与节奏

1. **Racing 先**，理由有两条且都硬：它是**唯一无 `Intl` 的宿主**（最能证伪方案），
   也是**唯一真的需要复数**的（最有动机把坑踩完）。
2. 其余项目**按自己的节奏迁，不设死截止**。设了也守不住 —— 而一个守不住的截止日
   只会让人学会无视它。
3. **不要为了统一而现在就动它们。** 今天动只有风险没有收益，
   而风险是「取词路径坏掉」这种全站级别的。

### 10.4 过渡期：两条路并存，代价写清楚

`i18next-preset` **保留**，标记为「将退役」。代价必须说明白：

- 🔴 **`check` / `analyze` 要同时认两种形态** —— 这是并存期的真实成本，不是零。
- 🔴 **框架自己就分叉了**，而本仓最恨这个。所以并存是**过渡态不是终态**，
  文档里要写明「最后一个消费方迁完就删」，并且**在 README 里留下这句话**，
  否则五个月后没人记得它本来是要删的。

### 10.5 判据：什么时候动下一个项目

**上一个项目跑满 §7 那三条验收，且真机/线上跑过一个版本。**
不是「代码合了」，是「用户用过了」——取词是全站路径，坏起来是白屏。

---

## 11. spike 结果（2026-09-09，`Racing-7` 分支 `i18n-icu-spike`，**未合并**）

### 三条验收

| # | 结果 | 证据 |
|---|---|---|
| ① 编译接得进 uni-app 构建 | ✅ **过** | h5 跑通且文案（含带参插值）全对；**`build:mp-weixin` 也构建成功** |
| ② 小程序**真机** | ⏳ **待跑** | 本机没装微信开发者工具。包已构建好在 `Racing-7/miniapp/dist/build/mp-weixin` |
| ③ 改 JSON → 浏览器立刻变 | ✅ **过** | 改一句文案、**不重启**，6 秒内页面就变；日志打出「文案表变了，已重编译」。**比现状好**（Wakuwaku 现在要重启整个 dev server）|

### 体积：比预估好得多

| | |
|---|---|
| `vendor.js` | 746 KB → **703 KB**（**−43 KB**，`i18next` 完全不在包里）|
| 文案表 | 31 KB → 41 KB（+10 KB，编译产物）|
| **净** | 🎉 **约 −33 KB** |

原文 §2 预估 +7 KB，**实测是省 33 KB** —— 因为算漏了「i18next 整个退出」。

### 🔴 spike 逼出来的两条，文档原来没写

**① 编译产物拿 `undefined` 会抛错。**
`t('app.nav.data')` 这种不传第二参数的调用**全仓到处都是**，不兜是**白屏**不是少个字。
→ `makeT` 里 `vars || {}` 兜住，并单独钉了测试。

**② 取词模块变得测不了。**
`index.js` import 虚拟模块 `virtual:i18n`，Node 直接跑挂在 `ERR_UNSUPPORTED_ESM_URL_SCHEME`。
→ 拆成**纯逻辑 `make-t.js` + 薄壳 `index.js`` —— **这正是本框架自己的成例**
（`src/check.js` 纯逻辑 + `tools/check.mjs` 薄 CLI）。
**所以框架该导出的是 `makeT` 而不是一个完整的取词模块**，这一条要写进 §10.2 的分工。

### 行为契约变化（要写进 USAGE）

漏传占位符的痕迹：`{round}` → `undefined`。两种都「一眼可见」，
原契约延续、只是形状换了。开发期哨兵两种都认。
